---
const { tag, title, description, image, url } = Astro.props;
---

<div class="card h-full">
  <button class="rounded-full ...">{tag}</button>
  <h3 class="text-3xl">{title}</h3>
  <p class="text-2xl">
    {description}
  </p>
</div>
