---
const { class: className, ...rest } = Astro.props;
---

<!-- When no class is passed to this component undefined gets added to class list. Fix so that undefined is not there -->
<div
  class=`flex flex-col items-start py-10 px-12 rounded-3xl bg-neutral-100 border border-gray-300 gap-8 ${className}`
  {...rest}
>
  <slot />
</div>
